# Class Notes

UI Framework
    - UI = f(state)
    - Declarative
    - Custom "components"
        - Pass "props" into components
        - reusable
        - using JSX
            - is *Transpiled* into javascript
    - Data binding (useState)

Vite: 
- transpiles JSX using ESBuild (or Babel)
- "Bundles" -> puts all code into a single file
- Provides a server
- Provides initialization tools